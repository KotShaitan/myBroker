# 📬 Брокер сообщений на C#

## 📌 Описание проекта

Проект представляет собой простой брокер сообщений, реализованный на языке C#.
Взаимодействие с брокером осуществляется через HTTP API.

Брокер позволяет приложениям обмениваться данными через очереди и топики:
производители (Producer) отправляют сообщения, а потребители (Consumer) получают и обрабатывают их.

---

## 🎯 Цели проекта

* Изучить принципы работы брокеров сообщений
* Реализовать модель Producer / Consumer
* Понять, как работают:

  * очереди (Queue)
  * топики (Pub/Sub)
  * доставка и подтверждение сообщений
* Освоить разработку HTTP API на C#

---

## ⚙️ Основные возможности

* Отправка сообщений через HTTP (Publish)
* Подписка на сообщения (Subscribe)
* Получение сообщений (Pull модель)
* Хранение сообщений в памяти
* Подтверждение обработки (ACK)

---

## 🌐 API (пример)

### 📤 Отправка сообщения

```
POST /publish
```

```json
{
  "topic": "orders",
  "payload": "data"
}
```

---

### 📥 Получение сообщений

```
GET /consume?topic=orders
```

---

### ✅ Подтверждение обработки

```
POST /ack
```

```json
{
  "messageId": "uuid"
}
```

---

## 🧱 Требования и ⚙️ Реализация

### Функциональные:

* Producer отправляет сообщения через HTTP, через http контроллер сообщение попадает в монолит с основной логикой
* Consumer подписывается POST /subscribe/orders и получает сообщения через HTTP 
* Брокер хранит и распределяет сообщения в топики и их очереди, очереди записываются в логфайлы
* Сам брокер состоит из 5 сервисов внутри монолита:
* * Topic-manager - создает очереди
  * Queue-manager - реализация самой очереди
  * Subscription-manager - обеспечивает взаимодействие между pub и sub
  * Delivery-manager - обеспечивает доставку сообщений
  * Manage Store - запись и хранение логов
* Поддержка подтверждения (ACK)

---

### Нефункциональные:

* Асинхронная обработка запросов
* Потокобезопасность
* Простота масштабирования
* Минимальные задержки

---

## ⚙️ Технологии

* C#
* .NET 10
* ASP.NET Core (HTTP API)
* System.Text.Json

---

## 🚀 Запуск

```bash
dotnet run
```

После запуска API будет доступно по адресу:

```
http://localhost:5000
```

---

## 📚 Назначение

Проект разработан в учебных целях для понимания принципов работы брокеров сообщений и построения HTTP API.
